#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Succes)
 */
int main(void)
{
	char MyStr[] = "\"Programming is like building a multilingual puzzle";

	puts(MyStr);

	return (0);
}
