#include <stdio.h>

long long int largest_prime_factor(long long int n);

/**
 * main - Entry point
 * Description: Test largest_prime_factor function
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	long long int number = 612852475143;
	long long int largest_factor = largest_prime_factor(number);

	printf("%llu", largest_factor)
	return (0);
}


/**
 * largest_prime_factor - prints the largest prime factor of a number
 * @n: number
 *
 * Return: largest prime factor
 */
long long int largest_prime_factor(long long int n)
{
	long long int largest factor = -1;

	return (largest_factor);
}

