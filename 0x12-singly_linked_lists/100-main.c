#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str = "(A tortoise, having pretty good sense of a hare's nature,"
		"challenges one to a race.)\n";

	printf("%s", str);

	return (0);
}

